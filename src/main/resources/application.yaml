spring:
  application:
    name: warehouse
  datasource:
    url: jdbc:postgresql://localhost:5432/travelexpense
    username: postgres
    password: anime021
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_schema: geli

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

info:
  title: Warehouse Management API
  version: 1.0
  description: API documentation

server:
  port: 9090